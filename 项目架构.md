# 项目架构

这份文档说清楚平台的整体结构、关键实现方式和技术选型。内容按 2026-09-19 仓库里的代码写，里面的版本号、路径、参数都是实测的。使用说明在 `Readme.md`，题目要求在 `项目实施方案.md`。

## 一、架构

前端是 Vue 3 的单页应用，跑在 3000 端口；后端是 FastAPI，跑在 8000。后端里的调度层按工作流图逐个叫起 AI Agent，每次调用就是起一个 `dsh --profile headless` 子进程跑一次会话。产物按账号落在 `backend/exports/u<用户>/p<项目>/` 下，SQLite 里只存状态和文件路径。

```
浏览器（Vue 3，:3000）
   │  /api、/previews、/avatars 由 Vite 转发到后端 :8000
   ▼
FastAPI 后端（:8000）
   ├── app/api/       36 条接口，收请求、认身份、查归属，然后交给下层
   ├── app/agents/    调度层：orchestrator 管状态和闸门，workflow_engine 跑图，ai_client 管和 DSH 的交互
   ├── app/runner/    生成应用的启停：项目自己的虚拟环境、8100 端口、进程账本、残留进程回收
   ├── app/storage/   paths 管所有路径，file_helper 管产物读写和打包
   ├── app/db/        SQLAlchemy + SQLite，7 张表
   └── app/core/      配置、token、启动守卫
        │
        ▼  每个节点起一个 dsh 无头会话（在 bubblewrap 沙箱里，独立进程组）
   DeepSeek Harness 命令行 ── 技能正文拼成提示词发给模型 ── 产出按 # File: 格式回传
        │
        ▼
   backend/exports/u<用户>/p<项目>/ = src/ + PRD.md + 任务书 + Test_Report.md + 运行日志/ + data/ + p<项目>.zip
```

## 二、技术栈

| 用途 | 选型 | 版本 | 说明 |
|---|---|---|---|
| 前端框架 | Vue 3 + TypeScript | vue 3.4+ / TS 5.9.3 | 用 `vue-tsc` 做类型检查，构建脚本先检查再打包 |
| 前端工程 | Vite | 5 | `npm run dev` 起开发服务器，改代码即时生效 |
| 状态与路由 | Pinia、vue-router | 3.0.4 / 4.6.4 | 5 个 store：auth、task、workflow、skill、ui |
| 编排画布 | @vue-flow/core | 1.48.2 | 拖节点连线做工作流，图存成 JSON 放在 `workflows.nodes` |
| 后端框架 | FastAPI + uvicorn | 0.141.1 / 0.52.4 | 自带 OpenAPI，依赖注入用来取身份和数据库会话 |
| ORM 与校验 | SQLAlchemy 2 + Pydantic v2 | 2.0.52 / 2.13.5 | 同步 ORM，接口出入参用 Pydantic 校验 |
| 数据库 | SQLite | `backend/data/project.db` | 单文件，7 张表，改表结构前自动备份一份 |
| AI 能力 | DeepSeek Harness（headless profile） | dsh 0.1.2-rc.1 | 平台不碰模型和上下文，只把技能正文和输入值交给命令行跑一次会话 |
| 技能体系 | `SKILL.md` | `.dsh/skills/` 下 12 个 | 提示词和输出格式都放在技能里，加技能只要加目录 |
| 隔离 | bubblewrap + 项目专属虚拟环境 | bwrap 0.11.1 | Agent 的 shell 在沙箱里跑，每个生成项目的依赖互不影响 |
| 运行环境 | Python / Node | 3.14.4 / v22.23.2 | 用 3.14 自带的 `compression.zstd` 读 DSH 会话事件流，不用另外装包 |

后端 Python 代码一共 9258 行（`main.py` 加 `app/`），其中 `app/agents/` 4188 行、`app/api/` 2220 行、`app/db/` 1181 行、`app/runner/` 871 行、`app/storage/` 453 行、`app/core/` 180 行。依赖清单在 `requirements.txt`。

## 三、系统架构

### 3.1 后端分包

调用方向只能往下走：

```
api/  →  agents/  →  db/  →  storage/
  └───────┴──────────┴──────→  core/
storage/ 和 core/ 不依赖上层
```

| 包 | 放什么 | 不放什么 |
|---|---|---|
| `api/` | 接口路由、出入参结构、身份依赖、归属校验 | 编排和调度逻辑 |
| `agents/` | 状态机、图执行、技能绑定、DSH 调用、运行日志、真跑脚本 | 直接写产物文件（走 storage） |
| `db/` | engine、models、crud、改表迁移、内置种子 | 磁盘目录和文件操作 |
| `storage/` | 路径常量、产物读写、打包 | 数据库操作 |
| `runner/` | 生成应用的进程和虚拟环境 | 平台自己的业务 |
| `core/` | 配置读取、token 签发校验、启动守卫 | 业务逻辑 |

各包里的主要文件：

- `api/`：`routes/` 下有 auth、projects、project_app、agents、skills、steps、workflow、planner 八个路由文件，另外有 `schemas/` 和 `deps.py`。身份解析只写在 `deps.current_user_id` 一处；应用启停那几个接口是同步函数，FastAPI 会把它们放到线程池里跑，不占事件循环。
- `agents/`：`orchestrator.py`（状态机、审批和交付闸门）、`workflow_engine.py`（图执行器）、`ai_client.py`（一次 DSH 会话）、`builtin_workflows.py`（内置图）、`node_inputs.py`（节点输入组装）、`planner_runner.py`（编排官）、`agent_author.py`（一句话生成提示词）、`run_registry.py`（运行中进程的注册表）、`runlog.py`（运行日志）、`skill_meta.py`、`agent_selftest.sh`。
- `db/`：`engine.py`、`models/` 六个模型文件、`crud/` 六个 crud 文件、`migrate.py`（加列和索引，改之前自动备份）、`seed.py`、`init_db.py`、`cleanup_history.py`。
- `storage/`：`paths.py`（所有路径都从这里取）、`file_helper.py`（产物读写、日志按白名单读、打包）、`migrate_exports.py`、`migrate_logs.py`。
- `runner/`：`venv.py`（建环境、装依赖）、`env.py`（端口和路径）、`process.py`（启停、就绪探测、账本、回收）。
- `core/`：`config.py`、`security.py`、`reload_guard.py`。

### 3.2 启动时做的事情

`backend/main.py` 在 lifespan 里按顺序做五件事：

1. 打开外键约束（SQLite 默认是关的，每个连接都要开一次）；
2. `create_all` 建表，表已存在就是空操作；
3. 检查自己是不是跑在 `--reload` 下，是就打一段横幅说明后果（见 4.11）；
4. 回收上一次平台退出时留下的生成应用进程，不然它们会一直占着 8100；
5. 把上次进程死亡时残留的 `RUNNING` 步骤和项目标成"被中断"，不然界面上会一直显示"进行中"。

另外注册了一个慢请求中间件：某个请求耗时超过 1 秒（阈值可以用 `DSH_SLOW_REQUEST_SECONDS` 改），就往日志里打一行，写清楚方法、路径、耗时，以及当时有没有项目在跑。最后挂上 `/previews`（指向 exports）和 `/avatars`（指向 data/images），注册全部路由。

### 3.3 数据库

7 张表：

| 表 | 存什么 | 主要字段 |
|---|---|---|
| `users` | 账号 | `user_name`（唯一）、`password`、`avatar` |
| `projects` | 项目 | `status`、`zip_path`、`mode`（workflow/agent）、`workflow_id`、`workflow_name`、`planned_workflow`、`pass_first_try` |
| `project_steps` | 每一步的执行轨迹，前端步骤卡片读它 | `round_no`、`step_no`、`name`、`status`、`artifact_path`、`session_id`、`elapsed_ms`、`started_at_ms`、`error_code`、`error` |
| `project_agents` | 每个 AI 角色干完活留下的成果 | `role`、`agent_name`、`path`、`final_output`、`session_id`、`elapsed_time` |
| `agents` | 可选的 AI 角色（内置加用户自定义） | `role_key`、`name`、`mode`、`skill_id`、`system_prompt`、`output_kind`、`user_id` |
| `workflows` | 用户自己保存的工作流 | `name`、`nodes`（JSON）、`builtin` |
| `sqlite_sequence` | SQLite 内部用的 | — |

内置角色 9 个：pm、architect、backend-executor、frontend-executor、simple-frontend、qa、reviewer、docgen、planner，都绑在对应的技能上；用户自己造的走 `mode='prompt'`，`role_key` 是 `custom_` 加八位随机串。内置工作流是代码里的常量，不存表。名字上区分一下：带 `project_` 前缀的表存的是做完事留下的成果，不带前缀的 `agents` 是角色菜单。

### 3.4 磁盘上的东西

```
backend/data/
├── project.db                       平台数据：账号、项目、步骤、角色、图
├── project.db.bak_<时间戳>           改表结构之前自动备份的
├── images/                          头像
├── logs/u<用户>/p<项目>/<会话号>.json  DSH 会话的原始 JSON，给机器看的
└── app_runtime.json                 生成应用的进程账本，运行时才有

backend/exports/u<用户>/p<项目>/
├── PRD.md、Test_Report.md、任务书、Plan.json   各 Agent 的文档产物
├── src/backend、src/frontend/                  生成的源码
├── data/                                       生成应用自己的数据
├── 运行日志/、项目BUG/                           平台生成给人看的日志
├── .venv/、.pip-cache/                         项目自己的环境和缓存
└── ../p<项目>.zip                              交付包
```

几条约定：

- crud 只管数据库，不碰文件；PRD、报告、代码这些大文本只在库里存路径，正文落盘；
- 生成应用的数据一律写在项目根的 `data/`（和 `src/` 同级），因为每轮重跑会清空 `src/`，写里面就丢了；
- 打包 ZIP 时排除 `运行日志/`、`项目BUG/`、`_logs/`、`.venv`、`node_modules`、`__pycache__`、`*.log`、`*.pyc`、`*.db`，以及会话捕获文件 `*.stdout`、`*.stderr`；
- 平台自己的会话日志放在 `data/logs/`（在项目目录外面），既不进交付包，也不会被静态目录暴露出去。

### 3.5 前端

| 位置 | 内容 |
|---|---|
| `src/views/` | 8 个页面：LoginView（登录注册）、HomeView（新建项目）、TaskListView（项目列表）、TaskDetailView（对话加右侧产物、预览、运行日志）、WorkflowView（编排画布）、SkillsView（技能和角色，路由 `/agents` 也用它）、StatsView（耗时看板）、SettingsView |
| `src/stores/` | 5 个 Pinia store：auth、task（含 5 秒轮询和终止、续跑）、workflow、skill、ui |
| `src/api/` | 接口封装，`http.ts` 里统一带 token，401 会清登录态 |
| `src/utils/` | 状态映射、预览地址拼装、应用动作等纯函数 |
| 代理 | `vite.config.ts` 把 `/api`、`/previews`、`/avatars` 转发到 `frontend/.env` 里的 `VITE_API_TARGET`，默认是 `http://127.0.0.1:8000` |

### 3.6 AI 调用层

`.dsh/skills/` 下有 12 个技能：pm-workflow、requirement-normalizer、simple-frontend、architect-planner、backend-executor、frontend-executor、qa-workflow、code-reviewer、doc-writer、planner、generic-prompt-agent、agent-prompt-authoring。每个技能用 `{{input.xxx}}` 写占位符，头部 frontmatter 里声明需要哪些输入、输出按什么格式给。

调用方式：`ai_client` 把技能正文和输入值拼成完整提示词，用 `subprocess.Popen` 起 `dsh --profile headless`，工作目录是这一步的临时目录，独立进程组。答案在 stdout，stderr 是噪声，会话结束后按契约解析。

有两个沙箱上的事实影响了实现方式：

- 后台进程不跨 bash 调用存活，所以"起服务、打接口、杀服务"必须在一次调用里做完；
- Agent 会照着提示词里的格式自己写一句 `[SELFTEST] PASS`，哪怕服务其实没起来，所以结论以脚本自己写下的记录为准，Agent 自述只当参考。

`new_venv/` 是 Agent 在沙箱里用的 python。沙箱里的 `python3` 是照 PATH 找的，本机 PATH 第一位就是 `new_venv/bin`，所以 Agent 的自检和起 uvicorn 真跑都靠它。这一条是环境凑出来的，不是代码保证的：换一台 PATH 里没有它的机器，Agent 的 `python3` 就是系统 python，自检门禁会失效。

## 四、实现方法

### 4.1 一次生成任务的流程

```
用户点创建项目
  → POST /api/projects/create 写一行 projects，建项目目录，记下编排方式和图
  → 后台跑 PRD 段：run_workflow(..., stop_after=PM 节点)，跑完项目置成待审批
用户点通过审批（有补充要求可以带上）
  → workflow 模式：run_workflow(项目选定的图, preset={PM: 已批准的结果})
    agent 模式：编排官读 PRD 出图（Plan.json），转成图之后再跑
        → 架构师拆任务书 → 简单前端直出，或者前后端并行开发 → QA 出测试报告
        → 每一步写一行 project_steps，记会话号、耗时、错误码
  → 收尾：写总览、打包 ZIP、过 QA 交付闸门、通过就自动启动应用，项目置完成
```

### 4.2 一次节点就是一次无头会话

`ai_client.py` 负责这一步，主要处理四件事：

- 拼提示词：技能正文加输入值回填，特别长的提示词走文件交接，不塞进命令行；
- 起进程：`dsh --profile headless`，独立进程组，stdout 和 stderr 分开落盘，方便出错时看原文；
- 解析产物：按技能声明的契约解析，`file_blocks` 就是 `# File:` 那种分段代码，`json_array` 是 JSON 数组，还有纯文本；
- 记结果：返回会话号、耗时、日志、错误码。

错误码会写进 `project_steps.error_code`，常见的有 `DSH_NOT_FOUND`、`DSH_LAUNCH`、`DSH_CMD`、`DSH_EXIT_NONZERO`、`DSH_TIMEOUT`、`DSH_STALLED`、`DSH_ABORTED`、`SKILL_NOT_FOUND`、`PLACEHOLDER_MISSING`、`CONTRACT_SOFTFAIL`、`ROUTE_MISSING`。只有可重试的错误码才重试，用户主动终止的 `DSH_ABORTED` 不会重试。

关于自动超时：默认是关的（`DSH_IDLE_TIMEOUT_SECONDS=0`）。试过两种判断会话是否还活着的办法都不成立：stdout 只在会话结束时才写，DSH 的事件流文件也是会话结束才落盘，拿它们当活跃信号会误杀正在跑的长会话（真机误杀过 3 个）。所以卡住了由用户在页面上点终止。

### 4.3 图执行器

- 图是数据：节点、连线、条件都在 JSON 里。内置图是代码常量，用户图存在 `workflows.nodes`，编排官现场出的图也是同一种格式，三种来源共用一套执行器。
- 输入组装只有一条路：读技能头部声明的输入列表，按名字一个个填。`system_prompt` 取自定义 Agent 的提示词，`user_requirement` 和 `prd_content` 取种子里的值，其他名字统一回落到上下文块（用户需求加上游节点的输出）。声明过的输入一定会填满，缺一个会直接抛错（以前 `code-reviewer` 漏了 `prd_content` 就炸过）。
- 产物按输出形态落盘：节点写了 `code_dir` 就落那个目录，一个文件都没落出来就判失败；写了 `artifact_file` 就落那个文件名；契约是代码块或 JSON 数组就落项目的 `src/`；纯文本落成 `<步骤名>.md`。
- 失败的定义是"Agent 没产出"，不是"格式不对"。格式问题降级成 `CONTRACT_SOFTFAIL`，这一步仍算成功，但日志里看得见。

### 4.4 条件分支

以前的判断写法有问题：`bind()` 取不到值会返回空串，于是"解析不出来"和"值不匹配"走到同一个分支，图里两条路都被跳过，结果一个代码文件没生成，项目却报完成还打了包。

现在的做法是把被条件引用的节点当成决策源：取值集合直接从图里收集（把所有 `eq` 收齐），执行前用 `task_note` 告诉这个节点字段只能取哪几个值，执行后校验取值在不在集合里，取不到或者越界就把这一步判失败（`ROUTE_MISSING`）。取值集合来自条件本身，每个取值一定有分支接住，两条路全跳过在结构上就不会发生了。条件的写法只认 `<节点id>.parsed.<字段>` 这一种，其他写法在校验图的时候直接拒掉。

### 4.5 两段执行和审批

两段用同一个 `round_no`，`step_no` 接着往下排。第二段用 `preset` 把 PM 节点接回图里，但不建步骤行也不重跑，所以"生成 PRD"全程只出现一次，PM 只跑一次。编排官如果又排了 PM 步骤，转图的时候会剔掉。

### 4.6 QA 交付闸门

收尾时读 QA 报告里的结论：

| 结论 | 处理 |
|---|---|
| 通过 | 项目置完成，打包，自动启动应用 |
| 不通过 | 项目置失败，写一份 `运行日志/98-交付未通过.md` 说明原因，不启动应用 |
| 有测试节点但报告拿不到结论 | 同样置失败，不默认算通过 |
| 图里没有测试节点（用户自定义图） | 按通过处理，但记一笔日志 |

起因是真机踩过一次：报告上明明写着不通过，平台却把项目标成完成还打了包，用户点开是白屏，等于把没验收的东西交付了。

### 4.7 真跑自检和两类日志

代码类节点的工作目录里会自动放一份平台的 `agent_selftest.sh`（在沙箱里叫 `.selftest.sh`），并要求 Agent 跑它。脚本在一次 bash 调用里跑完，自己判断两种模式：

- 目录里有 `main.py`：起 `uvicorn main:app`，逐个打接口，失败时打印服务端的真实报错和行号，过掉 site-packages 里的帧；
- 纯静态：起 `http.server`，探测目录里已有的资源，还有 HTML 里引用的每个 css/js，漏产出就是 404，另外用 `node --check` 解析每个 js。

两类日志都在项目目录里，多轮跑会带 `_r<轮次>` 后缀：`运行日志/` 一个节点一份，记技能、会话号、耗时、真跑次数、产物清单和原始输出；`项目BUG/` 一次真跑一份，记结论、命令和报错原文。结论以脚本写的记录为准。

### 4.8 终止运行

每个节点执行时把自己的进程组注册到 `run_registry`。用户点终止，后端就 `killpg` 掉整个进程组（孙进程一起），并置上中止标志。收尾时先看这个标志：用户主动终止的，项目标成"已终止"而不是失败，已经产出的东西保留。另一种情况是平台自己重启导致的中断，走启动时的对账逻辑，把残留的 `RUNNING` 标成"运行被中断"，并提示可以从当前进度继续。

### 4.9 生成应用的运行时

| 环节 | 做法 |
|---|---|
| 依赖 | 每个项目建自己的虚拟环境 `<项目>/.venv`，按 `src/backend/requirements.txt` 装，装完写一个 marker 文件记 requirements 的哈希，内容没变就不重装 |
| 端口 | 生成应用固定用 8100，同一时间只有一个；被别的项目占着就返回 409 让前端问要不要切换，被非平台进程占着就直接报错 |
| 启动 | 读环境变量 `PORT` 和 `APP_HOST`（不能写死 `127.0.0.1`），起 `uvicorn main:app`，然后探 `/health` 和 `/` 直到就绪，30 秒还没起来就终止并保留日志 |
| 账本 | 进程信息写 `backend/data/app_runtime.json`，判断进程是否还活着的时候用命令行指纹核对（`<...>/p<id>/.venv/bin/python -m uvicorn main:app`），避免 pid 复用认错 |
| 生命周期 | 在主线程里启动的子进程带 `PR_SET_PDEATHSIG`，平台挂了应用跟着挂；从工作线程启动的只设资源上限，由父进程判断用哪种；平台启动时会回收上次残留的进程 |

### 4.10 增量修改和重新生成

前端两个按钮对应后端的 `iteration` 参数：

- `incremental`：保留 `src/`，把上一版代码预置到节点的工作目录里，并在要求里追加增量协议（先读再改、只改要求的部分、保留没让改的功能、改完照样真跑）。收尾时对比前后快照，写一份 `运行日志/97-本轮改动.md`，列出新增、修改、删除了哪些文件。
- `regenerate`：先清空 `src/` 再按需求重写。

改代码这条链只重跑架构师、前后端和 QA，PM 和 PRD 不重跑。项目选的图记在项目上，迭代不会换回默认图。

### 4.11 防止卡死的几处处理

- 收尾不占事件循环：收尾里要建虚拟环境、装依赖、起应用，最慢能到 150 秒左右。这段本来是同步的，被异步流程直接调用时会把事件循环占住，uvicorn 连新连接都接不了，用户这时刷新页面就一直转圈。现在保留了同步版本给离线脚本用，异步流程统一走 `_finalize_async` / `finish_project_async`，整段丢到工作线程里执行。删项目时停应用那一步也这么处理了。
- 不用 `--reload`：开了 reload 之后监听端口的是父进程，重启子进程的那几秒没人接请求；平台每生成一个项目又会往 `exports/` 下写几百个 `.py`，会不停触发重载，正在跑的任务被打断。现在启动时会检测命令行里有没有 reload，有就打横幅，新建、审批、改代码、重跑这四个入口直接拒绝（要放行可以设 `DSH_ALLOW_RELOAD=true`）。`main.py` 的 `__main__` 分支默认不带 reload，要热重载得显式设 `DSH_DEV_RELOAD=1`，那时会自动排除 `exports/`。
- 慢请求看门狗：任何请求超过 1 秒都会记一行日志，收尾的每一段耗时也单独打（比如"收尾耗时 · 自动起应用（建 venv/装依赖/等就绪）42.10s"），出问题能直接看出卡在哪一段。
- 启动对账：回收残留的应用进程，把残留的运行状态标成中断。

### 4.12 隔离和权限

- Agent 执行在 bubblewrap 沙箱里，工作目录是临时目录，产物按契约回传；
- 目录按 `exports/u<用户>/p<项目>` 隔离，接口层有归属校验，不属于自己的返回 404，不泄露资源是否存在；
- 读文件类的接口（产物正文、运行日志）都先按登记过的路径或真实文件列表做白名单匹配再读，不做路径拼接，所以 `../` 这类构造进不来；
- 身份识别支持 `Authorization: Bearer <token>`，另外兼容 `?user_id=`。`AUTH_REQUIRE_IDENTITY=false` 的时候识别不到身份就放行，这是课程设计期间的折中，正式部署要改成 true；
- 模型的 API Key 在 dsh 自己的目录里（`/root/.dsh/.credentials.yaml`），不在仓库也不在数据库里。

## 五、接口

一共 36 条 `/api` 路径（43 个操作），另有一条根路径。

| 资源 | 数量 | 主要接口 |
|---|---|---|
| `/api/users` | 6 | 注册、登录、查改删、上传头像 |
| `/api/projects` | 22 | 创建、列表、详情、审批、改代码（增量或重新生成）、终止运行、下载 ZIP、PRD、测试报告、产物清单和正文、运行日志、预览地址、应用启停（`/app/start`、`/app/stop`、`/app/status`、`/app/logs`）、步骤（`/steps`、`/steps/latest`、`/steps/rounds`） |
| `/api/agents` | 6 | 列表、增删改、一句话生成提示词、用量 |
| `/api/workflows` | 8 | 列表、增删改、校验、执行、编排官出图、用量 |
| `/api/skills` | 1 | 技能清单 |
| `/` | 1 | 健康信息 |

## 六、目录

```
/opt/multi agent/
├── backend/
│   ├── app/{api,agents,db,storage,runner,core}/
│   ├── main.py        入口：建表、启动守卫、对账、慢请求中间件、挂路由和静态目录
│   ├── .env           对外主机名、超时阈值
│   ├── data/          project.db、头像、会话日志、进程账本
│   └── exports/u<用户>/p<项目>/ 加 p<项目>.zip
├── frontend/
│   ├── src/{api,stores,views,components,utils}/
│   ├── index.html、vite.config.ts、package.json、tsconfig.json、.env
│   └── node_modules/
├── .dsh/skills/       12 个技能
├── new_venv/          后端运行环境，也是 Agent 沙箱里的 python3
├── requirements.txt
└── Readme.md、项目架构.md、项目实施方案.md
```

## 七、已知问题

1. 权限只做到能识别身份，没做完整的鉴权；`AUTH_REQUIRE_IDENTITY` 默认是 false，密码也是明文存的。正式用要开身份强制，密码要加盐哈希。
2. Agent 的 `python3` 靠宿主 PATH 指向 `new_venv`，换机器会失效。彻底解决要让代码执行跑在自带依赖的容器里。
3. 单机单实例：SQLite 单文件、固定 8100 端口，没有容器化和 CI。隔离靠 bubblewrap 加项目自己的虚拟环境。
4. 会话没有强制时长上限，看门狗默认关闭，卡住靠用户点终止。
5. 生成质量取决于模型，平台只保证流程走得通、每一步确实真跑过。
