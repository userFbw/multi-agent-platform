# 题目 29「2026 AI 原生全栈应用开发平台」实施方案

团队 5 人：前端 1 人（陈伟锋）、后端 2 人（方博闻、吴智攀）、Harness / AI 组 2 人（翁圳欣、卢树杰）。
时间安排：9.1 – 9.6 环境准备，9.7 – 9.23 开发，9.24 答辩。

## 一、平台做什么

用户在页面上写一句中文需求，平台依次叫起多个 AI Agent，做出一个能直接运行的全栈应用：PRD、前后端代码、测试报告、ZIP 包和在线预览。

已经做完的功能：

- 一句需求生成应用，纯前端和前后端分离都支持
- 两种编排方式：自己选模板或在画布上连线（workflow），或者审批后由编排官读 PRD 现场出图（agent）
- PRD 生成后停下来等人工审批，通过才继续开发，PM 全程只跑一次
- 代码写完在沙箱里真跑自检（起服务逐个打接口 / 探静态资源引用），报错由 Agent 当场改
- QA 报告判不通过就不交付：项目标失败、写明原因、不启动应用
- 前后端分离项目跑完自动启动一次，页面上能直接打开、也能下载 ZIP
- 两种迭代方式：增量修改（保留上一版代码局部改，附本轮改动清单）、重新生成（清空重写）
- 随时能终止：杀掉整个进程组，项目标成已终止而不是失败，已完成产物保留
- 自定义 Agent：写一句描述生成提示词，过目确认后落库，就成了画布上能拖的节点
- 全过程可追溯：步骤轨迹、每个 Agent 的运行日志、每次真跑的报错原文、耗时看板

## 二、总体架构

```
Vue 3 前端（:3000）
  └─ /api、/previews、/avatars 转发到后端
       FastAPI 后端（:8000）
         ├─ api 接口层        收请求、认身份、查归属
         ├─ agents 调度层     orchestrator 状态机与闸门 + workflow_engine 图执行器 + ai_client 会话封装
         ├─ runner 运行层     生成应用的项目 venv、8100 端口、启停与回收
         ├─ storage / db      产物读写打包；SQLite 7 张表
         └─ core              配置、token、启动守卫
              │
              └─ 每个节点 = 一次 dsh --profile headless 会话（跑在 bubblewrap 沙箱里）
产物：backend/exports/u<用户>/p<项目>/ = 源码 + PRD + 任务书 + 测试报告 + 运行日志 + ZIP
```

三个关键点：AI 能力全在 DeepSeek Harness 里，后端只按"输入/输出字段契约"调用；编排是数据化的，内置模板、用户画布图、编排官现场出的图走同一套执行器；状态存 SQLite，产物按账号隔离落盘，库里只存路径。

## 三、技术栈

| 层 | 选型 | 版本 |
|---|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Pinia + vue-router + vue-flow | vue 3.4+ / vite 5 / TS 5.9 |
| 后端 | Python + FastAPI + SQLAlchemy 2 + Pydantic v2 + uvicorn | Python 3.14.4 / fastapi 0.141.1 |
| 数据库 | SQLite（7 张表，改表前自动备份） | 单文件 `backend/data/project.db` |
| AI 层 | DeepSeek Harness（headless profile），12 个技能 | dsh 0.1.2-rc.1 |
| 隔离 | bubblewrap 沙箱 + 每个生成项目独立 venv | bwrap 0.11.1 |

## 四、实现要点

- **编排引擎**：图是 JSON，节点按依赖波次执行；节点输入按技能头部声明的字段逐个回填，产物按输出形态落盘（代码目录 / 指定文件 / 项目 `src/` / Markdown）。
- **两道闸门**：PRD 后的人工审批；收尾时的 QA 交付闸门（判不通过或拿不到结论一律不交付）。
- **真跑自检**：平台把自检脚本放进代码节点工作目录，Agent 在沙箱里真起服务、真打接口。实测 Agent 会照着格式虚报"自检通过"，所以结论以脚本自己写下的记录为准，报错原文存进 `项目BUG/`。
- **条件分支**：分支取值集合从图里的条件反推，执行前注入节点、执行后校验，非法取值判失败，避免两条路都被跳过却报成功。
- **终止与对账**：运行中的进程组登记在注册表里，终止时整组杀掉并标记；平台重启后把残留的"进行中"状态标成被中断。
- **迭代**：增量修改保留 `src/` 让 Agent 局部改并输出改动清单；重新生成先清空再写。两条路都不重跑 PM。
- **稳定性**：禁用 `--reload`（重载窗口里请求没人接，且平台写导出目录会触发重载风暴，四个写入口直接拒绝）；收尾的建环境、装依赖、起应用整段丢到工作线程，不占事件循环；请求超 1 秒和收尾各段耗时都落日志。
- **应用运行层**：项目专属 venv 按依赖哈希跳过重装；固定 8100 端口，被占返回 409 让用户决定；起服务后探 `/health` 直到就绪，超时则终止并留日志。

## 五、分工

| 小组 | 人数 | 成员 | 负责范围 |
|---|:---:|---|---|
| Harness / AI 组 | 2 | 翁圳欣、卢树杰 | DSH 环境与凭证；12 个技能的提示词与输出契约；统一 AI 调用封装（会话、契约解析、错误码、重试、会话登记）；真跑自检协议；编排官拆解与 Plan 契约 |
| 后端组 | 2 | 方博闻、吴智攀 | 数据库 7 张表与 CRUD；全部接口（users / projects / agents / skills / steps / workflow / planner）；编排引擎后端侧（状态流转、两道闸门、终止、迭代）；文件落盘与 ZIP 打包；应用运行层；稳定性处理与服务器部署 |
| 前端组 | 1 | 陈伟锋 | 8 个页面（工作台、画布、技能与角色、耗时看板、设置等）；5 秒轮询与契约字段对接；预览地址拼装与应用控制条；演示素材 |

各组具体交付：

- **Harness / AI 组**：bubblewrap 与 headless profile 就绪并冒烟通过；12 个 SKILL.md（产品经理、需求规范化、简单前端、架构师、后端、前端、QA、代码审查、文档、编排官、通用模板、提示词撰写）；`ai_client` 的会话启动、输入构造、契约解析、错误码与重试；真跑自检脚本与取证约定；编排官的两阶段（出图 → 跑图）。
- **后端组**：表结构与幂等迁移、CRUD；全部接口含审批、改代码、终止、产物清单与正文、运行日志、预览地址、应用启停；图解析与校验、按波次执行、状态回写、两道闸门、两种迭代；目录隔离与打包规则；项目 venv、8100 端口、就绪探测、进程账本与残留回收；禁 reload 守卫、收尾异步化、中断对账、慢请求日志；服务器部署与联调。
- **前端组**：任务工作台（左对话与步骤流、右产物/预览/运行日志）、编排画布（含条件分支编辑）、技能与角色页、耗时看板、设置；步骤卡片各状态展示；预览地址按浏览器主机名拼装、应用启停控制条；演示页面打磨与视频素材。

## 六、进度

| 时间 | 内容 | 状态 |
|---|---|---|
| 9.1 – 9.6 | 部署既有系统，安装 bubblewrap，初始化 headless profile，配置凭证并跑通一条会话 | 已完成 |
| 9.7 – 9.12 | 从 Dify 迁到 DSH；角色注册表与查表驱动；统一调用封装；技能扩充；编排官拆解 | 已完成 |
| 9.13 – 9.19 | 任务工作台与步骤可视化；真跑自检与运行日志、项目 BUG；编排画布与条件分支；审批与交付闸门；终止运行；应用运行层与在线预览；耗时看板 | 已完成 |
| 9.20 – 9.23 | 评测数据收口、设计报告、演示视频、部署演练、联调收尾 | 进行中 |
| 9.24 | 现场演示与答辩 | — |

## 七、交付物

- 源代码（GitHub）：前后端工程、数据化编排引擎、12 个技能、真跑自检协议、应用运行层、Readme 与架构文档
- 服务器部署链接：平台可直接体验全部功能
- 设计报告：架构、核心模块、实测数据（生成时长、真跑结论）
- 演示视频（5 分钟）：生成应用、自定义 Agent 与画布编排、迭代与可观测性